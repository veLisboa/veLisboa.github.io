One-line: the rule replaces cards — reach for it whenever you want to separate or group content.

```jsx
<Rule />
<Rule weight="heavy" tone="strong" />
```

hairline between list items; medium between sections; heavy only as a page or slide opener, directly above a display heading. Accent tone is the one place the slate appears structurally.
