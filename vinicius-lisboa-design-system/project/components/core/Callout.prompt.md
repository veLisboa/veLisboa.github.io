One-line: an aside for a caveat or practitioner note, built from a 2px rule and an uppercase label — never a tinted or bordered box.

```jsx
<Callout label="Na prática">Capacity F2 não sustenta refresh incremental acima de 50M linhas.</Callout>
```

Use `tone="accent"` at most once per article.
